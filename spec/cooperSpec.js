describe("Cooper", function() {
  var assistant;
  var track;

  /* beforeEach(function() {
    assistant = new Assistant({stopwatch: 12, whistle: true});
    track = new Track({distance: 400});
  }); */

  it("athelete warms up for 10 minutes", function() {
    expect(warm_up).toEqual(true);
  });

});
