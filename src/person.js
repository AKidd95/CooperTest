function Person(attr) {
  this.gender = attr.gender;
  this.age = attr.age;
  this.distance = attr.distance;
};

Person.prototype.calculateCooper = function() {
cooper = new Cooper();
cooper.calculateCooper(this);
};
